{
  "name": "backend",
  "version": "1.0.0",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "build": "encore build",
    "start": "encore run --env=production"
  },
  "dependencies": {
    "encore.dev": "^1.48.8"
    // Outras dependências que seu backend possa ter, ex: "express", "pg", etc.
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "encore": "^1.48.8" // É importante que 'encore' esteja acessível para o npx
    // Outras ferramentas de desenvolvimento que seu backend possa usar
  }
}
