{
  "name": "backend",
  "version": "1.0.0",
  "type": "module",
  "packageManager": "npm",
  "scripts": {
    "build": "encore build",
    "start": "encore run --env=production",
    "dev": "encore run",
    "frontend-build": "cd ../frontend && bun install && vite build --outDir=../backend/frontend/dist"
  },
  "dependencies": {
    "encore.dev": "^1.48.8"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
