{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd backend && npm install && npm run build", // <-- MUDANÇA AQUI
    "watchPatterns": ["backend/**"]
  },
  "deploy": {
    "startCommand": "cd backend && encore run --env=production",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300
  }
}
