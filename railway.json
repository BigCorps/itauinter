{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd backend && npm install && npm install -g @encoredev/cli && encore build",
    "watchPatterns": ["backend/**"]
  },
  "deploy": {
    "startCommand": "cd backend && npm install -g @encoredev/cli && encore run --env=production",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300
  }
}